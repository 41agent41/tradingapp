# Development / CI-only dependencies for the IB service.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
pytest>=7.4
ruff>=0.6
black>=24.8
httpx>=0.27   # FastAPI TestClient dependency for the observability tests
