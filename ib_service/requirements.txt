# TWS API Service Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
ibapi==9.81.1.post1
pydantic==2.5.2
python-multipart==0.0.6

# Data Analysis & Technical Indicators
pandas>=2.0.0
numpy>=1.24.0

# Basic utilities
python-dateutil==2.8.2
pytz==2023.3

# Phase 4 — real-time pipeline
redis>=5.0.1

# B2a — MetaTrader (MT5) sidecar HTTP client. Pinned <0.28 to match the
# TestClient shim already used in dev (0.28 dropped the app= shortcut).
httpx>=0.27,<0.28

# Phase 6 — observability (structured logging + Prometheus metrics)
structlog>=24.1.0
# Capped below 7.0: instrumentator 7.x/8.x require starlette>=0.30, which
# conflicts with the starlette<0.28 that fastapi==0.104.1 pins. 6.1.0 is the
# latest release compatible with starlette 0.27 (keeps the existing FastAPI).
prometheus-fastapi-instrumentator>=6.1.0,<7.0.0