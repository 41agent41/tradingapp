"""Jesse strategies shipped with the service (Jesse's ``strategies/`` folder).

Every module here that defines a ``jesse.strategies.Strategy`` subclass is
discovered by ``jesse.registry`` and published to the app's strategy catalogue
under ``jesse_<snake_case_class_name>``. Add a strategy by adding a module.
"""
